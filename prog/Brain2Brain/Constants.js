/**
 * How frequently should the game poll the server for a status update?
 */
var POLL_INTERVAL = 250; // Milliseconds

/**
 * How long should the game wait between seeing the "Run Ended" signal and refreshing the page?
 */
var REFRESH_WAIT = 5000; // Milliseconds

/**
 * TMS port
 */
var TMS_PORT = 25000;
